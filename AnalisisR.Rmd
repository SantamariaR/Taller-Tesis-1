---
title: "Análisis bayesiano de galaxias EAGLE"
author: "Ramiro Santamaria"
output:
  html_document:
    theme: flatly
    toc: true
    toc_float: true
    code_folding: show
  pdf_document: 
    latex_engine: lualatex
subtitle: "Comparación de historias evolutivas por fusionnes"
header-includes:
  - \usepackage{fvextra}
  - \DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,commandchars=\\\{\}}
---

```{r setup, include=FALSE}
rm(list = ls())
gc()

knitr::opts_chunk$set(
  echo    = TRUE,
  warning = FALSE,
  message = FALSE,
  fig.align = "center",
  out.width = "100%"
)

library(MASS)
library(tidyverse)
library(data.table)
library(rethinking)
library(brms)
library(bayesplot)
library(scales)
library(ibUBA)

set.seed(42)

# Paleta del curso
pal <- ib_palette()
col_azul    <- unname(pal["primary"])
col_rojo    <- unname(pal["accent"])
col_purpura <- unname(pal["purple"])
col_celeste <- unname(pal["blue"])
col_verde   <- unname(pal["green"])
col_gris    <- "#95A5A6"
```






```{r}
# Descargamos los datos que generamos
mi_data <- fread("data/AGN_Merger_A.csv")
mi_data

```












This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 

```{r}
plot(cars)
```

Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Ctrl+Alt+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Ctrl+Shift+K* to preview the HTML file).

The preview shows you a rendered HTML copy of the contents of the editor. Consequently, unlike *Knit*, *Preview* does not run any R code chunks. Instead, the output of the chunk when it was last run in the editor is displayed.
